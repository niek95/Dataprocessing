svg.selectAll("text")
 .data(dataset)
 .enter()
 .append("text")
 .text((d) => {
   return d[0];
 })
 .attr("x", (d,i) => {
   return i * ((w - sidePadding * 2) / dataset.length) + sidePadding;
 })
 .attr("y", h - topPadding)
 .attr("transform", "rotate(-10)");
